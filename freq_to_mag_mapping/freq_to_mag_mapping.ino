/*

Objectives:

1. Produce a function to map X, Y, and Z frequencies into a numerical representation of a magnetic field

*/

int freqToMag() {
    int bigB = 0; // Set magnetic field to 0 to start 

    // Pass frequencies from X, Y, and Z axi
    // Use the equation: B = a + bf + cf^2

    return bigB;
};